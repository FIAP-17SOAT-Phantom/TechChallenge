name: CI - Build, Test e Cobertura

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-test-unitarios:
    name: Build + Testes Unitarios + Cobertura
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET 8
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Restore
        run: dotnet restore src/OficinaMecanica.slnx

      - name: Build
        run: dotnet build src/OficinaMecanica.slnx --no-restore --configuration Release

      - name: Testes unitarios com cobertura
        run: >
          dotnet test src/OficinaMecanica.Tests/OficinaMecanica.Tests.csproj
          --no-build
          --configuration Release
          --collect:"XPlat Code Coverage"
          --results-directory ./coverage
          --logger "trx;LogFileName=unit-tests.trx"

      - name: Instalar ReportGenerator
        run: dotnet tool install --global dotnet-reportgenerator-globaltool

      - name: Gerar relatorio de cobertura
        run: >
          reportgenerator
          -reports:./coverage/**/coverage.cobertura.xml
          -targetdir:./coverage/report
          -reporttypes:"Html;TextSummary;MarkdownSummaryGithub"

      - name: Publicar resumo de cobertura
        if: always()
        run: cat ./coverage/report/SummaryGithub.md >> $GITHUB_STEP_SUMMARY

      - name: Upload relatorio de cobertura
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: ./coverage/report

  testes-integracao:
    name: Testes de Integracao (TestContainers)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET 8
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Restore
        run: dotnet restore src/OficinaMecanica.slnx

      - name: Build
        run: dotnet build src/OficinaMecanica.slnx --no-restore --configuration Release

      # O runner Ubuntu ja tem Docker instalado - TestContainers sobe o PostgreSQL automaticamente
      - name: Testes de integracao
        run: >
          dotnet test src/OficinaMecanica.IntegrationTests/OficinaMecanica.IntegrationTests.csproj
          --no-build
          --configuration Release
          --logger "trx;LogFileName=integration-tests.trx"

  scan-vulnerabilidades:
    name: Scan de Vulnerabilidades
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET 8
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Restore
        run: dotnet restore src/OficinaMecanica.slnx

      - name: Scan de pacotes vulneraveis
        run: dotnet list src/OficinaMecanica.slnx package --vulnerable --include-transitive
